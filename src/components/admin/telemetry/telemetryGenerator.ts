import { LiveVisitorSession, LiveTelemetryEvent } from '../../../types/telemetry';

export function generateSeedSessions(count: number = 0): LiveVisitorSession[] {
  // Returns empty array to guarantee 100% real visitor data only
  return [];
}

export function generateInitialEvents(): LiveTelemetryEvent[] {
  // Returns empty array to guarantee 100% real visitor telemetry events only
  return [];
}

